{
  "name": "student-permission-leave-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Student Permission & Leave Management System with React, Express, MySQL, and JWT authentication.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
